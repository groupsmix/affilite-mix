{
  "name": "affilite-mix",
  "version": "1.0.0",
  "scripts": {
    "db:types": "supabase gen types typescript --local > types/database.ts",
    "db:drift-check": "npm run db:types && git diff --exit-code types/database.ts"
  },
  "dependencies": {
    "next": "15.0.0",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
