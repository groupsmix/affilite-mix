# dependencies
node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
Thumbs.db
Desktop.ini
.idea/
.vscode/
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# cloudflare
.open-next/
.wrangler/
.dev.vars
cf-snapshot-*/

# playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
wrangler.preview.json

# terraform
infra/**/*.tfstate
infra/**/*.tfstate.backup
infra/**/.terraform/
infra/**/.terraform.lock.hcl
infra/**/terraform.tfvars
infra/**/*.tfplan
terraform/**/*.tfstate
terraform/**/*.tfstate.backup
terraform/**/.terraform/
terraform/**/.terraform.lock.hcl
terraform/**/terraform.tfvars
terraform/**/*.tfplan

# sbom (generated at build time)
sbom.cdx.json
sbom.spdx.json
sbom.*.bundle

# security audit (keep out of public repos — see W5)
SECURITY-AUDIT.md
supabase/.temp/

# audit F-016: gitleaks binary/archive must not be vendored
gitleaks
gitleaks_*.tar.gz
gitleaks_*.zip
coverage/

# Playwright global-setup storage state (generated at runtime)
e2e/.auth/
