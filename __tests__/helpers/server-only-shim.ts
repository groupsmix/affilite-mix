// Empty stub for "server-only" used by vitest. Next.js replaces the
// real `import "server-only"` at compile time; this stub stands in
// during tests so server-only modules can be imported.
export {};
