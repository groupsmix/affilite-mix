import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Cloudflare Pages deployment via @opennextjs/cloudflare
  // No experimental features needed for Slice 1
};

export default nextConfig;
